# DocSense web auth

React + TypeScript frontend for the Stitch authentication and OTP screens.

```bash
npm install
npm run dev
```

Run the API separately from `api/` on `http://localhost:3000`.
Set `VITE_API_URL` when the API is hosted elsewhere.
